

# 端口
server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog_myself?serverTimezone=GMT&characterEncoding=utf-8&useSSL=false&nullCatalogMeansCurrent=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    com:
      demo:
        xwx:
          mapper: debug

